{name: unpack, on: push, permissions: {contents: write}, jobs: {unpack: {runs-on: ubuntu-latest, steps: [{uses: "actions/checkout@v4"}, {run: "if [ -f site.zip ]; then unzip -oq site.zip; rm site.zip .github/workflows/ci.yml; git config user.name github-actions; git config user.email actions@github.com; git add -A; git commit -m 'Add Nugawela Villas site source'; git push; fi"}]}}}
